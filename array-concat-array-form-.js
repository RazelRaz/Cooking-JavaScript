const colors = ['red', 'green', 'black'];
const fruits = ['apple', 'berry'];

const add = colors.concat(fruits)
console.log(add);


const content = "Quick brown fox."
const maleAnArray = Array.from(content);
console.log(maleAnArray[9]);